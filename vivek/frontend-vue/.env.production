# Production build: use same-origin (relative /api) — the FastAPI server serves
# both the SPA and the API. Empty value falls through to the runtime hostname
# check in the app code (localhost in dev, same-origin in deployment).
VITE_API_BASE_URL=
